#pragma once

namespace intel_driver_resource {
    // Assuming this is a placeholder for binary driver data that gets built during compilation
    // This will be populated by the bin2header tool during build
    extern const unsigned char driver[];
    extern const size_t driver_len;
}
